# /tusk-gnome-clean-run

Run `make clean run` in the project root to do a clean build and launch Tusk.

## Steps

1. Run `make clean run` from `/home/sri/Work/ShapeMachine/tusk-gnome`
2. Report success or any build errors to the user
